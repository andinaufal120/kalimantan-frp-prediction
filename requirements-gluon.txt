# CPU-only AutoGluon tabular env. After `pip install -r requirements-gluon.txt`,
# run: pip uninstall -y nvidia-nccl-cu12
# (it's an unconditional xgboost wheel dependency, unused without a GPU).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
autogluon.tabular[all]==1.5.0
numpy==2.3.5
pandas==2.3.3
python-dateutil==2.9.0.post0
